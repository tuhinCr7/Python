#  tupple are immuteable that's mean it's can't change

tup = (3, 5, 'tuhin', 5)
print(tup)
print(type(tup))

#  count it's will count how many of this in a tuple

print(tup.count(5))
#  finding length of a tuple

print(len(tup))
